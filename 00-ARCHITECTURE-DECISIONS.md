# قرارات معمارية محسومة — قبل بناء الـ Schema

> هذه القرارات تحسم التعارضات في وثيقة السياسات وتُعتمد كمرجع للـ Backend.

---

## 1. التسعير — تغيير جوهري ⚠️

### القرار المعتمد
النسبة **ليست ثابتة عالمياً (60%)** — بل **الأدمن يحددها لكل باقة/دورة عند الموافقة**.

### التدفق الفعلي

```
المدرس / المركز
  → يُنشئ باقة ويضع سعره الخاص (teacher_price)
  → يرسلها للمراجعة (pending_approval)
       ↓
الأدمن
  → يراجع الباقة
  → يُحدد نسبة المنصة لهذه الباقة تحديداً (platform_margin_percent)
  → يوافق
       ↓
النظام
  → يحسب: student_price = teacher_price × (1 + margin/100)
  → الباقة تصبح active وظاهرة للطلاب بالسعر النهائي
```

### الأثر على قاعدة البيانات
- `packages.teacher_price` — يُدخله المعلم
- `packages.platform_margin_percent` — **يُدخله الأدمن عند الموافقة** (nullable حتى الموافقة)
- `packages.student_price` — يُحسب عند الموافقة ويُخزَّن (snapshot، لا يتغير بعدها)
- **لا** نستخدم GENERATED COLUMN لأن النسبة تُحدد لاحقاً وليست ثابتة
- الافتراضي المقترح في config: `default_platform_margin = 60` (مجرد قيمة مقترحة تظهر للأدمن، قابلة للتعديل)

### تصحيح RULE-04
النص القديم: ~~"السعر الظاهر للطالب = سعر المعلم × 60% دائماً"~~

**النص الجديد:**
> السعر الظاهر للطالب = سعر المعلم + نسبة المنصة التي يحددها الأدمن عند الموافقة على الباقة. النسبة تُخزَّن مع الباقة ولا تتغير بأثر رجعي.

---

## 2. رؤية الأسعار

| الطرف | يرى سعر المعلم | يرى السعر النهائي | يرى نسبة المنصة |
|---|---|---|---|
| Admin | ✅ | ✅ | ✅ |
| Teacher | ✅ (سعره) | ✅ | ❌ |
| Student | ❌ | ✅ | ❌ |

**تصحيح المصفوفة 9.3:** المعلم **يرى** السعر النهائي للطالب (كان ❌ فأصبح ✅)، لكن **لا يرى** النسبة نفسها ولا عائد المنصة.

---

## 3. تغيير المواعيد — موافقة الأدمن إلزامية دائماً

### القرار المعتمد
**كل** طلب تغيير موعد يمر بموافقة الأدمن — سواء خلال 24 ساعة أو بعدها. **لا يوجد تغيير تلقائي.**

### تصحيح القسم 6.2
النص القديم: ~~"الموعد يتغير تلقائياً بدون موافقة إضافية"~~

**النص الجديد:**
> جميع طلبات تغيير المواعيد تدخل قائمة انتظار الأدمن. الفرق بين ما قبل 24 ساعة وما بعدها هو في **الأولوية والسبب**: خلال 24 ساعة السبب اختياري وتُعالَج بأولوية عالية؛ بعد 24 ساعة السبب إلزامي وتخضع لتقدير الأدمن.

### الأثر على قاعدة البيانات
جدول `reschedule_requests` يحمل دائماً حالة `pending` عند الإنشاء — لا مسار تلقائي.

---

## 4. مهلة الإشعار المسبق للغياب

**القرار:** 6 ساعات (وليس 2).

- الطالب يُبلغ قبل **6 ساعات أو أكثر** → الجلسة لا تُحتسَب من رصيده، تُعاد جدولتها.
- أقل من 6 ساعات → تُعامَل كغياب بدون إشعار.

Config: `advance_notice_hours = 6`

---

## 5. الأدمن يستطيع الحجز نيابةً عن طالب

**القرار:** ✅ مفعّل.

### الحالات
- الطالب يواجه مشكلة تقنية في الدفع
- حجز يدوي بعد تسوية شكوى
- تعويض عن جلسة ملغاة

### الأثر على قاعدة البيانات
جدول `bookings` يحمل:
- `created_by_admin_id` (nullable) — من أنشأ الحجز إن كان أدمن
- `is_manual` (boolean) — حجز يدوي بدون دفع Stripe
- `manual_reason` (text, nullable) — سبب الحجز اليدوي

كل حجز يدوي يُسجَّل إلزامياً في `audit_logs`.

**تصحيح المصفوفة 9.4:** "إنشاء حجز" → Admin ✅ (بمبرر مسجَّل).

---

## 6. المراكز التدريبية والدورات

**القرار:** تُبنى الجداول الآن.

نفس منطق التسعير ينطبق: المركز يضع سعر الدورة → الأدمن يحدد نسبته عند الموافقة → يُحسب السعر النهائي.

---

## 7. أنماط الحجز الثلاثة (من دليل المصمم)

| النمط | الجدول | الاختيار | جدول قاعدة البيانات |
|---|---|---|---|
| فردي (مدرس) | مرن | الطالب يختار من `availability_slots` | `bookings` بموعد فردي |
| مجموعة (مدرس) | ثابت مشترك | ينضم لجدول موجود | `package_schedules` + `bookings` بنفس الموعد |
| دورة (مركز) | صارم | لا اختيار | `course_schedules` + `enrollments` |

---

## 8. قيم Config النهائية

```php
'default_platform_margin_percent' => 60,   // مقترح للأدمن، ليس إلزامياً
'reschedule_requires_admin'       => true, // دائماً
'advance_notice_hours'            => 6,
'no_show_grace_period_minutes'    => 15,
'no_show_final_minutes'           => 30,
'review_window_days'              => 7,
'review_edit_window_hours'        => 24,
'package_expiry_grace_days'       => 14,
'invitation_link_expiry_hours'    => 48,
'booking_payment_hold_minutes'    => 15,
'disconnect_grace_minutes'        => 10,
```
